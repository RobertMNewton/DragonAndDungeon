import pygame


if __name__ == "__main__":
    # initialise pygame
    pygame.init()

    # create drawing window
    window = pygame.display.set_mode(size=(1920, 1080))

    # main game loop
    running = True
    while running:
        pass
